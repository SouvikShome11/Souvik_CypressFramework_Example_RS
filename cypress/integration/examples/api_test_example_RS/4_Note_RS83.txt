pure API testing (basic) with Cypress without depending on Web(Front end) part. Generally it is not recommended to use Cypress
if your goal is anywhere on Non Web Automation.

But you may need at times to call external third Party Services which are not tied up to your Web, So I have added one basic
video  to show how to perform only API testing using Cypress for knowledge Purpose.